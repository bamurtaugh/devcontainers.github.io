{
	// VS Code debugger configuration for containers.dev tooling
	"version": "0.2.0",
	"configurations": [
		{
			"type": "node",
			"request": "launch",
			// Debug configuration for the specification markdown generator
			// Converts TypeScript spec definitions into formatted documentation
			"name": "Generate specification.md",
			"program": "${workspaceRoot}/spec-generator/generator.ts",
			"outFiles": [ "${workspaceRoot}/spec-generator/out/**/*.js" ]
		}
	]
}